#include <iostream>
#include <algorithm>
using namespace std;
int main(){
    int result =4;
    int s=0;
    int out=0;
    for(int i = 0;i<result;i++){
        int a,b;
        cin >> a >> b;
        s+=(b-a);
        out = max(out,s);
    }
    cout << out << endl;
}
