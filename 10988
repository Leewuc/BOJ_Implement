#include <iostream>
#include <string>
using namespace std;
int main(){
    string n;
    cin >> n;
    for(int i=0;i < n.size()/2;i++){
        if(n[i] != n[n.size()-i-1]) {
            cout << "0" << '\n';
            return 0;
        }
    }
    cout << "1" << '\n';
}
