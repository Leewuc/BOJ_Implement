#include <iostream>
#include <string>
using namespace std;
string n,m;
int main(){
    cin >> n >> m;
    string a;
    for(int i=2; i>=0;i--){
        if(n[i]>m[i]) {
            a= n;
            break;
        }
        else if(n[i]<m[i]){
            a = m;
            break;
        }
    }
    for(int i=2;i>=0;i--) {
        cout << a[i];
    }
}
