#include <iostream>
#include <cstring>
using namespace std;
int main(){
    char n[1000005];
    int last = 0;
    cin >> n;
    if(n[0 != ' ']) last++;
    for(int i=1; i<strlen(n);i++){
        if(n[i-1]==' ' && n[i] != ' ') last++;
    }
    cout << last;
        
}
