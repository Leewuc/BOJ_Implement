#include <iostream>
using namespace std;
int main(){
    int a,b;
    int x,y;
    cin >> a;
    cin >> b;
    for(int i=0;i<b;i++){
        cin >> x >> y;
        a -= x*y;
    }
    if(a==0) cout << "Yes";
    else cout << "No";
}
